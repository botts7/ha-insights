{
  "config": {
    "step": {
      "user": {
        "title": "Set up HA Insights",
        "description": "HA Insights observes your home state to suggest automations. Pick how (or whether) the LLM enrichment surface works. You can change this later in Configure.",
        "data": {
          "llm_mode": "LLM mode"
        },
        "data_description": {
          "llm_mode": "Off: pattern detection only, no LLM calls. Local: route Explain / Refine / Hypothesize to a local LLM (Ollama, Wyoming) — data stays on your network. Cloud: pseudonymized data is sent to your configured cloud LLM (Anthropic, OpenAI, Google). Real names never leave; sensitive attributes are stripped."
        }
      },
      "cloud_consent": {
        "title": "Cloud LLM — confirm data flow",
        "description": "Cloud LLMs (Anthropic, OpenAI, Google) live outside your home network. With pseudonymization on by default, real entity names and area names never leave; numeric values are bucketed; sensitive attributes (GPS, MAC, tokens, passwords) are always stripped. Every outbound call is recorded in the privacy log sensor.\n\nLocal LLM is recommended if available. Continue with cloud anyway?",
        "data": {
          "cloud_consent": "I understand and want to enable cloud LLM enrichment"
        },
        "data_description": {
          "cloud_consent": "Required to enable cloud mode. You can switch back to Off or Local at any time."
        }
      }
    },
    "abort": {
      "already_configured": "HA Insights is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA Insights settings",
        "description": "Tune privacy mode, notifications, the daily digest, and your preferred LLM agent. Switching INTO Cloud LLM mode requires re-confirming the data-flow consent.",
        "data": {
          "llm_mode": "LLM mode",
          "lookback_days": "Recorder backfill lookback (days)",
          "notify_on_insight": "Notify on high-confidence insight",
          "notify_threshold": "Notification confidence threshold (0.0 - 1.0)",
          "digest_enabled": "Send a daily digest notification",
          "digest_hour": "Digest hour (0 - 23, local time)",
          "preferred_agent_id": "Preferred LLM agent (auto-pick if blank)",
          "refine_cost_threshold_usd": "Refine cost confirm threshold (USD)",
          "allow_user_detectors": "Allow user-supplied detectors (advanced)"
        },
        "data_description": {
          "llm_mode": "Off: pattern detection only. Local: data stays on your network. Cloud: pseudonymized data sent off-network.",
          "lookback_days": "How many days of HA recorder history to backfill at startup so detectors have data immediately. 0 disables backfill. Range 0-30; default 14 covers the schedule and seasonality detectors.",
          "notify_on_insight": "Fire a persistent_notification the moment a new insight lands above the confidence threshold below.",
          "notify_threshold": "Minimum confidence (0.0-1.0) for an insight to trigger an immediate notification. Default 0.8 means only very confident proposals notify; lower it to be alerted on more.",
          "digest_enabled": "Send one persistent_notification per day with a rollup of new and still-open insights. Auto-suppressed when there is nothing to report.",
          "digest_hour": "Hour of day (0-23, local time) to fire the daily digest. Default 9 = 9 AM.",
          "preferred_agent_id": "Which conversation agent to try first for Explain, Refine, and Hypothesize. Failover walks the other installed agents if it fails. Leave blank to auto-pick (Assist default first, then registry order). With multiple HA Insights entries, the first non-empty preference wins integration-wide.",
          "refine_cost_threshold_usd": "When a Refine call's estimated USD cost exceeds this, the card shows a confirm dialog before sending. 0 = always confirm, 10 = effectively never. Local agents always cost $0 and never trigger the confirm. With multiple HA Insights entries, the lowest threshold wins so a cautious entry isn't bypassed by a permissive one.",
          "allow_user_detectors": "Off by default. When on, *.py files in <config>/ha_insights_detectors/ load as community detectors. Each file is AST-scanned and rejected if it imports network/filesystem/subprocess modules or uses eval/exec/__import__, but the sandbox is best-effort — only enable for files you trust. See docs/writing-a-detector.md."
        }
      },
      "cloud_consent": {
        "title": "Confirm cloud data flow",
        "description": "Pseudonymized data will be sent to your configured Cloud LLM agent. Real names never leave; sensitive attributes are stripped.",
        "data": {
          "cloud_consent": "I understand and want to enable cloud LLM enrichment"
        },
        "data_description": {
          "cloud_consent": "Required when switching INTO cloud mode. You can switch back to Off or Local at any time."
        }
      }
    }
  }
}
